schema: http://167.71.146.35:4466/
overwrite: true
generates:
  src/apollo/types.ts:
    config:
      withHooks: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
documents:
  - src/**/*.gql
